<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.zhaizz.subject.mapper.SubjectMapper">

    <!-- 按关键词搜索（匹配 name 和 name_cn） -->
    <select id="searchByKeyword" resultType="top.zhaizz.pojo.entity.Subject">
        SELECT *
        FROM subject
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
           OR name_cn LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY IF(name_cn LIKE CONCAT('%', #{keyword}, '%'), 0, 1),
                 score DESC
    </select>

    <!-- 按播出日期范围查询 -->
    <select id="findByAirDateRange" resultType="top.zhaizz.pojo.entity.Subject">
        SELECT *
        FROM subject
        WHERE air_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY air_date
    </select>

    <!-- 按标签名查询条目 ID 列表 -->
    <select id="findSubjectIdsByTag" resultType="java.lang.Long">
        SELECT subject_id
        FROM subject_tag
        WHERE name = #{tagName}
        ORDER BY id
    </select>

</mapper>
