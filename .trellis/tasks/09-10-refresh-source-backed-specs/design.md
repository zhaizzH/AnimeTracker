# 审计方案

保留 backend/frontend/guides 三个目录。按当前源码修正失实陈述，在独立 Agent 运行契约中集中说明角色路由、Prompt 快照、thinking 流和能力自述的限制。各层索引链接到所有适用主题。

事实优先级：当前实现及测试、配置与 CI、版本化迁移、历史记录。历史结论只附日期引用；规范期望与未落实的实现分别标注。源码中的错误不会自动成为推荐规范。

用户已确认全部规范刷新范围；本次不改业务实现，不操作运行服务和数据库。
