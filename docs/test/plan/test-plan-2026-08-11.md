# AnimeTracker「番组手账」测试计划（第五轮 · 全量回归 · v1.1.0 规范）

> 版本：v3.0（2026-08-11）
> 被测 commit：6015eca（fix: admin dashboard图表卡被压成1/3宽, chart-*类选择器特异性不足被.dash-grid>.panel覆盖）
> 基线 commit：9749df0e77（round-3 全绿 153/153，无 P0，存于 baseline-success-sha.txt）
> 制定方：Hermes Agent（按 project-test-pipeline skill v1.1.0 新建，不复用旧计划）
> 适用：本机 192.168.0.3，源码运行 local profile
> 执行方式：hermes 主会话执行，L4 浏览器实测 + 截图检查为强制项

---

## 0. 执行说明

1. **先读 docs**：接口文档 `docs/spec/openapi.yaml`、数据库 `docs/database/db-schema.sql` 已核对；controller 端点已用 rg 复核（`/api/client/collections/{subjectId}/save` 等，与文档一致）。
2. **顺序执行**：Smoke Gate → L0 → L2 → L1 → L3 → L4，逐层串行；L0 失败率 >10% 阻断高层（准入阻断）。
3. **失败分类**：环境问题 → LIMITED 不阻塞（附补测条件）；代码缺陷 → FAIL 按 P0-P3 分级记录并进入修复环节。
4. **测试账号**：client `test1/123456`，admin `admin/123456`；禁止中途注册（撞限流）。
5. **数据一致性**：L3/L4 做「前端页面 ↔ 接口返回 ↔ 数据库」三方核对；L1/L4 对 AI 助手生成文本做第四方核对（LLM 回答 ↔ 接口 ↔ DB ↔ 前端展示）。
6. **Flaky 治理**：间歇性失败标 FLAKY 独立统计，不占用常规修复循环；占比 >5% 触发上线风险提示。
7. **产出**：`docs/test/report/test-report-2026-08-11.md`，修复不自动提交留人工 review（用户偏好）；测试通过后先向用户展示报告总结，用户确认后才提交。

## 1. 测试目标

1. 全量回归 L0-L4，验证 6015eca 无回归（相对基线 9749df0 的变更：agent 收藏类型修复 ×3 文件、前端移动端卡片布局 ×2、admin dashboard grid ×1）。
2. 重点回归本轮变更影响区：
   - agent 收藏类型语义（1=想看 2=看过 3=在看 4=搁置 5=抛弃）四方一致
   - 移动端收藏页卡片布局（390px 视口无遮挡）
   - admin dashboard 图表卡宽度比例（chart-lg 应占 2/3）
3. 历史缺陷回归：A3 防爆破、C2 重复收藏 409、D8 agent health、IP 漂移配置。

## 2. 测试范围

| 层 | 含 | 不含 |
|---|---|---|
| L0 后端 Java 单测 | app/src/test 全部（33 条） | 新增测试框架 |
| L1 Agent pytest | tests/ 全部（100 条）+ AI 文本四方核对 | 真实 LLM 长对话 |
| L2 前端构建 | client/admin tsc + vite build（4 项） | 组件单测 |
| L3 API 冒烟 | 登录/防爆破/收藏/409/health/列表/详情/type 过滤/dashboard（10 条） | 压测 |
| L4 E2E 浏览器 | client + admin 实测 + 截图检查（桌面+移动视口）+ AI 助手文本核对（6 条） | 并发 |
| L5 可选层 | 混沌/性能/SAST | 不启用（用户未显式指令） |

## 3. 环境与前置条件（Smoke Gate 已过）

| 依赖 | 端口 | 状态 |
|---|---|---|
| MySQL 8.4.9 (Docker) | 3306 | 运行中，8 表契约与 db-schema.sql 一致 |
| Redis 8.8.0 (Docker) | 6379 | 运行中（db=1），登录失败计数已清 |
| MinIO | 9000 | 运行中 |
| business 后端 | 8080 | 运行中（登录 200） |
| agent 服务 | 8090 | 运行中（/docs 200） |
| client 前端 (nginx) | 80 | active（200） |
| admin 前端 (nginx) | 81 | active（200） |

测试账号：test1/123456（USER，id=1）、admin/123456（ADMIN，id=0）。
数据基线（scripts/test-seed.sql 重置，幂等验证 ×2）：user_collection 5 条（type=3 ×4：14749/12563/1/15111；type=1 ×1：14149），user 3 条（admin/test1/test2）。
环境指纹：JDK 21.0.11、Maven 3.9.9、Node 24.19.0、MySQL 8.4.9、Redis 8.8.0、swap 7.9G、local profile。

## 4. 用例清单

### L0 Java 单测（1 项，33 条）

| ID | 步骤 | 期望 |
|---|---|---|
| L0-ALL-001 | `mvn test`（MAVEN_OPTS=-Xmx1g） | 33 通过 0 失败 |

### L1 Agent pytest（1 项，100 条）+ AI 文本核对（1 条）

| ID | 步骤 | 期望 |
|---|---|---|
| L1-ALL-001 | `pytest tests/ -q`（先 unset CORS_ORIGINS） | 100 通过 0 失败 |
| L1-AI-001 | 登录 test1 建会话，问「我的收藏列表是什么」流式读 answer | 回答含「在看 4 部」且列出 4 部（无职转生/天子传奇/皮丘与皮卡丘/尼古喵喵），类型标签与 DB type=3 一致；不得出现「看过」误标 |

### L2 前端构建（4 项）

| ID | 步骤 | 期望 |
|---|---|---|
| L2-CLIENT-001 | client `npx tsc -b` | exit 0 |
| L2-CLIENT-002 | client `npx vite build` | built 成功 |
| L2-ADMIN-001 | admin `npx tsc -b` | exit 0 |
| L2-ADMIN-002 | admin `npx vite build` | built 成功 |

### L3 API 冒烟（10 条）

| ID | 步骤 | 期望 |
|---|---|---|
| L3-AUTH-001 | 正确密码登录 test1 | code=200 + token |
| L3-AUTH-002 | 连续 6 次错误密码 | 均 401 |
| L3-AUTH-003 | 第 7 次正确密码 | 401「登录失败次数过多」 |
| L3-COL-001 | 收藏 subject 2 type=3 | 200 |
| L3-COL-002 | 重复收藏同 type | 409「该条目已收藏」 |
| L3-COL-003 | collections?type=3 | 返回 4 条（与库一致） |
| L3-COL-004 | 收藏后清理测试数据 | 库恢复 5 条基线 |
| L3-AGENT-001 | GET /api/client/agent/health | llm_configured=true |
| L3-ADMIN-001 | admin dashboard collection-stats | type=1:1, type=3:4 |
| L3-SUB-001 | 无 token GET subjects 列表 | 200（permitAll 预期） |

### L4 E2E 浏览器实测（强制，6 条，每页必截必看）

| ID | 步骤 | 期望 |
|---|---|---|
| L4-CLIENT-001 | 浏览器登录 test1 → 首页 | 显示用户名；截图检查无布局异常 |
| L4-CLIENT-002 | 打开 my-collections | 全部 tab 5 条；截图检查 |
| L4-CLIENT-003 | 切「在看」tab | 4 条，与库 type=3 一致；截图检查 |
| L4-CLIENT-004 | 打开番剧详情页（14749） | 评分/简介/剧集列表渲染；截图检查 |
| L4-ADMIN-001 | 浏览器登录 admin 进仪表盘 | 收藏类型「想看 1 在看 4」；**图表卡宽度比例检查**（chart-lg 应占 2/3 宽，gridColumn=span 8）；截图检查 |
| L4-ADMIN-002 | 三方核对 + 移动端视口 | 页面数量 = 接口 = 数据库；390px 视口 client 收藏页卡片布局无遮挡、操作按钮可见可点 |

**L4 强制检查项**：
1. 每页 `page.screenshot()` 后必须视觉检查截图本身（布局比例/裁切/遮挡/错位/溢出），不只依赖 DOM 数值。
2. 移动端视口 390×844（iPhone）：`document.documentElement.scrollWidth > window.innerWidth` 即溢出；表格/操作列/按钮必须在视口内可见。
3. 布局比例检查：`getComputedStyle(el).gridColumn` / `getBoundingClientRect().width` 与设计意图比对（dashboard chart-lg 应 span 8 而非被压成 span 4）。
4. AI 助手生成文本与真实数据四方核对（L1-AI-001 在浏览器端复验）。

## 5. 缺陷分级与退出标准

**缺陷分级**：
- P0 阻断：核心链路崩溃、登录失败、数据错乱、写入脏数据 → 必须修复，不放行
- P1 严重：功能可用但核心场景异常、移动端不可操作、AI 语义映射错误 → 优先修复，可协商降级开关
- P2 一般：次要布局瑕疵、非关键交互、文案错误 → 不阻塞上线，登记技术债
- P3 低优：微调视觉、非关键提示、文档问题 → 进 backlog

**退出标准**：无未处置 P0；P1 有结论（修复或明确降级方案）；L4 浏览器实测 + 截图检查必须完成。

## 6. 报告模板

1. 基础信息：环境指纹（OS/JDK/Maven/Node/Docker/中间件/swap/profile）、被测 SHA、基线 SHA、触发人、起止时间
2. 分层四态计数 + 通过率、耗时拆解（L0-L4 各层耗时）
3. 缺陷清单按 P0/P1/P2/P3 分级，失败明细必写原因 + 日志片段
4. Flaky 清单、受限明细（含补测条件）、未覆盖模块风险清单
5. 变更影响摘要、上线建议（放行 / 受限放行 / 阻塞）、回滚触发条件
6. 审计链路（修复 Agent、进程 ID、调度日志）
