# AnimeTracker 测试计划 v7（round-7 / r3）

## §0 AI 执行者说明

- 被测 commit：`a10276c`（本地=远程）；基线：`72da11c`（round-6 全量 162/162 全绿点）
- 端点以实际代码为准，执行前 `rg "Mapping\(" / "PostMapping\("` 核对；响应包装 `Result<T>{code,message,data}`，code=HTTP 状态码
- 严格按层串行：Smoke → L0 → L2 → L1 → L3 → L4；状态四态 PASS/FAIL/SKIP/LIMITED（环境问题标 LIMITED 不阻塞）
- 测试账号预置（admin/123456、test1/123456），禁止中途注册
- 报告不写 token/密钥；默认不自动提交，留人工 review
- agent 聊天 SSE 请求体字段是 `content`（ChatRequest: session_id + content），不是 `message`

## §1 目标与范围

**本轮变更影响（72da11c..HEAD，10 文件）**：
1. 导入页统计卡：`ImportServiceImpl`/`ImportStatusVO`/`ImportServiceImplTest` — 卡片1 改为任务总数（import_record 全量 `totalLogs`）、成功/失败任务全量 DB 聚合；构造器回退 3 参；`AdminEpisodeMapper` 删除
2. 前端：`ImportTasks.tsx`（卡片标签「任务总数」+ `totalLogs` 字段）、`types/api.ts`、`mock/admin.ts`
3. `Login.tsx` — 终端光标只保留最后一行（`index === feedLines.length - 1`）
4. `openapi.yaml` — totalLogs 文档 2 处
5. docs：round-6 plan/report（162/162）

**验收标准**：
- 导入页四卡：任务总数 == import_record 全量 count；成功任务 == status=COMPLETED 全量；失败任务 == status=FAILED 全量；最近导入 == recentRecords 首条 completedAt；三方核对（页面↔接口↔DB）一致
- 登录页终端光标有且仅有一个，位于最后一行「[提示]请使用管理员账号登录」之后
- 全量回归：L0 35、L2 4、L1 100+AI、L3 接口契约、L4 E2E 视觉（桌面+移动+布局比例+AI 文本四方核对）
- 深挖专项：导入运行链路（触发→RUNNING→COMPLETED/FAILED）、失败记录 errorMessage 展示、导入并发 409、热门榜 collectionCount>0、dashboard chart-lg 布局

**不纳入**：L5 混沌/性能/SAST（需显式指令）；MinIO 图片外网可达性（P2 #11 已登记，本机 47.96.127.231 不可达）

## §2 环境与准入条件

- 机器 192.168.0.3（Debian 13, 3.8G RAM+7.9G swap）；服务：business 8080（手动 jar）、agent 8090（uvicorn main:app）、nginx 已停（测试期间直连端口）
- Docker：MySQL 8.4.9(:3306)、Redis 8.8.0(:6379, db=1)、MinIO(:9000)、Postgres(:5432)、青龙（无关）
- 数据基线（2026-08-11 测试前清理后）：subjects=10614、episodes=148782、import_record COMPLETED=18/FAILED=5（总23）、user_collection=5（type=3×4: 14749 尼古喵喵 rate8 / 12563 无职转生 rate1 ep6 / 1 皮丘与皮卡丘 ep1 / 15111 天子传奇 ep1; type=1×1: 14149 碧蓝之海）
- Smoke Gate：agent /docs 200、business login 200、import/status 200、Redis PONG、MySQL 可查
- 契约：8 表结构与 db-schema.sql 一致（已核）

## §3 测试数据管理

| fixture | 用途 | 破坏性 | 清理 |
|---|---|---|---|
| admin/test1/test2 + 5 行 user_collection | 全量用例 | 是（收藏变更） | 重放 `docs/test/test-seed.sql` |
| import_record 23 条历史 | 统计卡断言 | 触发导入会新增 | 记录前后 count 差值 |
| Redis `auth:login-fail:*` | 防爆破用例 | 是 | 测试后 `-n 1 del` |

## §4 用例清单（共 52 个）

### L0 单元测试（8）
| ID | 优先级 | 步骤 | 期望 |
|---|---|---|---|
| L0-IMPORT-001 | P0 | `cd backend/business && mvn -pl app -am test` | BUILD SUCCESS；surefire 聚合 Tests run=35, Failures=0, Errors=0 |
| L0-IMPORT-002 | P1 | 检查 ImportServiceImplTest 断言 | 存在 totalLogs 断言（全量 count），无 totalSubjects 残留 |
| L0-IMPORT-003 | P1 | rg "totalSubjects" backend/ frontend/ docs/spec/ | 0 命中（mock/admin.ts 已改 totalLogs） |
| L0-IMPORT-004 | P1 | 检查 ImportServiceImpl 构造器 | 3 参（restTemplate, agentProperties, importRecordMapper） |
| L0-LOGIN-001 | P1 | rg "line.tone" Login.tsx | 光标条件为 `index === feedLines.length - 1` |
| L0-HOT-001 | P1 | DashboardMapperTest | 存在 hotSubjectsMapCollectionCount 用例，断言 collectionCount>0 |
| L0-409-001 | P1 | CollectionServiceImplTest | 存在同类型同评分 resubmit → 409 用例 |
| L0-LOGIN-002 | P2 | Login.tsx tsc 无 error | 0 类型错误 |

### L2 前端组件（2）
| ID | 优先级 | 步骤 | 期望 |
|---|---|---|---|
| L2-ADMIN-001 | P0 | admin: `npx tsc -b` + `npx vite build` | tsc 0 error；build 成功产出 dist |
| L2-CLIENT-001 | P0 | client: `npx tsc -b` + `npx vite build` | tsc 0 error；build 成功产出 dist |

### L1 Agent 能力（4）
| ID | 优先级 | 步骤 | 期望 |
|---|---|---|---|
| L1-PYTEST-001 | P0 | `cd backend/agent && env -u CORS_ORIGINS ./venv/bin/python -m pytest tests/ -q` | 全部 passed（28 文件 ~100 用例） |
| L1-AI-001 | P1 | SSE 问「我的收藏列表」（test1） | answer 流返回：4 部在看（无职转生/天子传奇/皮丘与皮卡丘/尼古喵喵）+ 1 部想看（碧蓝之海）；文本中「看过」出现 0 次 |
| L1-AI-002 | P1 | 四方核对 | AI 文本状态标签 ↔ 接口 collection_states ↔ DB type（3=在看/1=想看）↔ 前端展示一致 |
| L1-AI-003 | P2 | SSE 问「我最喜欢什么类型」等空值问题 | 不报错、answer 合理兜底 |

### L3 接口契约（18）
| ID | 优先级 | 步骤 | 期望 |
|---|---|---|---|
| L3-AUTH-001 | P0 | admin/test1 login | 200, code=200, data.token 非空 |
| L3-AUTH-002 | P1 | 错误密码×5 后正确密码 | 第 6 次前被限流拒绝（429/423），测后清 Redis |
| L3-IMPORT-001 | P0 | GET /api/admin/import/status（admin） | data.totalLogs == import_record 全量 count（当前 23）；completedCount==18；failedCount==5；recentRecords ≤10 且按 startedAt 倒序 |
| L3-IMPORT-002 | P0 | 三方核对 | 接口数值 == `SELECT status,COUNT(*) FROM import_record GROUP BY status` |
| L3-IMPORT-003 | P1 | GET /api/admin/import/records?page=1&size=10&status=FAILED | 分页 total==5；每行 status=FAILED、errorMessage 非空 |
| L3-IMPORT-004 | P1 | POST /api/admin/import/run mode=recent（无运行中任务时） | 200 触发；import_record 新增 RUNNING 记录；随后（≤5 分钟）变 COMPLETED 或 FAILED |
| L3-IMPORT-005 | P1 | RUNNING 期间再 POST run | 409 + message 含「已有导入任务运行中」 |
| L3-IMPORT-006 | P1 | run mode=season 无 key | 400 参数校验 |
| L3-IMPORT-007 | P1 | run mode=bogus | 400 参数校验 |
| L3-IMPORT-008 | P1 | client token 调 admin /api/admin/import/status | 401/403 权限拒绝 |
| L3-COLL-001 | P0 | test1 收藏 14749（同 type=3 rate=8 重复提交） | 409（同类型同评分） |
| L3-COLL-002 | P1 | test1 收藏 14749 type=3 rate=9（改评分） | 200 更新成功 |
| L3-COLL-003 | P1 | test1 收藏 14749 type=1（改类型） | 200 更新成功 |
| L3-HOT-001 | P1 | GET /api/admin/dashboard/hot | data[0..4].collectionCount 全 >0 |
| L3-DASH-001 | P1 | GET /api/admin/dashboard/overview | importCount 等字段类型正确、无 null 崩 |
| L3-ADMIN-LOG-001 | P2 | GET /api/admin/logs 分页 | 200，total>0 |
| L3-SUBJECT-001 | P1 | GET /api/admin/subjects?page=1&size=10 | 200，total==10614 |
| L3-SCHEDULE-001 | P2 | GET /api/client/subjects/schedule | 200，data 数组非空 |

### L4 E2E 视觉（20）
| ID | 优先级 | 步骤 | 期望 |
|---|---|---|---|
| L4-ADMIN-LOGIN-001 | P0 | 桌面 1440×900 打开 :81 → admin 登录 | 进入控制台；终端光标仅 1 个且位于最后一行「[提示]请使用管理员账号登录」后；截图存证 |
| L4-ADMIN-LOGIN-002 | P1 | 移动 390×844 打开 :81 登录页 | 布局无溢出（scrollWidth ≤ innerWidth）；光标 1 个；截图 |
| L4-ADMIN-IMPORT-001 | P0 | admin → 数据导入页 | 四卡：任务总数 23 / 成功任务 18 / 失败任务 5 / 最近导入非空；页面数字==接口==DB；截图 |
| L4-ADMIN-IMPORT-002 | P1 | 导入历史表格 | 共 23 条记录、分页 3 页；失败行显示红色错误信息；console 0 error |
| L4-ADMIN-IMPORT-003 | P1 | 当前任务区 | 无运行中任务时显示「空闲」 |
| L4-ADMIN-DASH-001 | P0 | admin → 仪表盘 | 6 面板正常渲染；热门榜 collectionCount 显示 1/1/1/1/1（非 0）；截图 |
| L4-ADMIN-DASH-002 | P1 | getComputedStyle 检查 chart-lg | `.dash-grid > .panel.chart-lg` gridColumn 占 2/3（span 8）；热门榜表格无内部横向滚动遮挡 |
| L4-ADMIN-DASH-003 | P2 | 移动 390×844 仪表盘 | 无溢出；卡片可读 |
| L4-ADMIN-SUBJECT-001 | P1 | admin → 番剧管理 | 列表渲染、分页正常、搜索可用 |
| L4-CLIENT-001 | P0 | client :80 → test1 登录 | 登录成功进入主页 |
| L4-CLIENT-COLL-001 | P0 | test1 → 我的收藏 | 5 行；「在看」tab 4 行；数据与 DB 一致；截图 |
| L4-CLIENT-COLL-002 | P1 | 移动 390×844 收藏页 | 表格操作列在视口内可见可点；无横向溢出 |
| L4-CLIENT-HOME-001 | P1 | 主页统计 | 番剧总数 == 接口 == DB（10614）；截图 |
| L4-CLIENT-DETAIL-001 | P1 | 打开某番剧详情 | 详情渲染、收藏按钮状态与 DB 一致 |
| L4-CLIENT-AI-001 | P1 | 页面 AI 助手问「我的收藏列表」 | 回答文本状态标签与 DB/接口一致（在看4/想看1，无「看过」） |
| L4-MOBILE-001 | P1 | 全站 390×844 巡检 | 无页面级溢出（scrollWidth ≤ innerWidth），核心操作可达 |
| L4-CONSOL-001 | P1 | 全页面 console error 检查 | 0 error（client+admin 各页面） |
| L4-IMPORT-RUN-001 | P1 | 页面点「启动导入」（recent 模式） | 出现新任务；任务总数 +1；完成后成功任务 +1（或失败+1）；再截图核对 |
| L4-VISUAL-001 | P0 | 全部核心页面截图 vision 检查 | 无裁切/遮挡/错位/比例异常 |

## §5 执行顺序与缺陷流程

Smoke → L0 → L2 → L1 → L3 → L4（L4 内按上表顺序，最后 L4-VISUAL-001 汇总检查全部截图）
缺陷：记录（ID/层/证据）→ 修复（3-Agent：Hermes 调度、Claude 复杂业务/前端、Codex 样板/防护用例；新进程新上下文）→ 回归（先受影响用例+冒烟，再全量）。P0/P1 修复必须配套新增自动化防护用例。
环境问题 → LIMITED 附补测条件。

## §6 风险与退出标准

- 风险：导入触发耗时（full 模式禁止——已清理卡死 full 任务，本轮只用 recent）；AI 流耗时；内存紧张（MAVEN_OPTS -Xmx1g）
- 退出标准：无未处置 P0；P1 有结论（修复或登记）
- 阻断标准：business/agent 不可用且 30 分钟无法恢复 → 停该层

## §7 报告模板（引用）

test-report-2026-08-11-r3.md：环境指纹（OS/JDK/Maven/Node/Docker/profile/swap）、被测 SHA a10276c、基线 72da11c、分层四态计数+耗时、P 级缺陷清单（含日志片段）、Flaky 清单、受限明细、变更影响、上线建议（放行/受限放行/阻塞）、回滚条件、审计链路（修复 agent 记录）。
